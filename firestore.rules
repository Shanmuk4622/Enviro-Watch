rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to all documents for now
    match /{document=**} {
      allow read: if true;
    }

    // Allow write access to the devices collection only
    match /devices/{deviceId} {
      allow write: if true;
    }
  }
}